# Vite environment variables for CloudUnify Pro Frontend
# IMPORTANT: Only variables prefixed with VITE_ are exposed to the client.

# Base URL of the backend API, including version prefix if applicable.
# Example for local development with the backend on port 3001:
VITE_API_BASE=http://localhost:3001/v1
