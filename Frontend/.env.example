# CloudUnify Pro Frontend - Environment Example for Vite
# Copy this file to `.env` and set values for your environment.
# VITE_ variables are exposed to the client in Vite-based apps.

# Base URL for the Backend REST API (e.g., https://api.example.com/v1)
VITE_API_BASE=http://localhost:3001/v1
