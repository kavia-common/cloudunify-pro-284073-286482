# CloudUnify Pro Frontend environment
# PUBLIC: Variables prefixed with VITE_ are exposed to the browser.
# Set the base URL of the backend API (no trailing slash).
# For local development, the backend is expected on port 3001.
VITE_API_BASE=http://localhost:3001
